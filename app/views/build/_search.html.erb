<% cards.each do |card| %>
  <div class="card">
    <img src="https://s3-us-west-1.amazonaws.com/deckproxy.com/scans/<%= card.edition.downcase %>/<%= card.number %>.jpg" />
  </div>
<% end %>
<input type="hidden" id="loadMore" value="<%= load_more ? offset : 'none' %>" />
